---
- name: Format the partitions
  community.general.filesystem:
    dev: '{{ item.src }}'  # `item.src` is the actual device. See `plan` module for more information
    fstype: '{{ item.fstype }}'
  loop: '{{ keystone.mount_plan }}'
